#include <stdio.h>
#include <stdlib.h>

int main(void) {
    double esp;
    int *p, i;
    scanf("%le", &esp);

    for(*p = esp; *p; p++) {
        if(*(esp) == 0) {
            break;
        }
        if()
    }

    printf("%f", esp);
}